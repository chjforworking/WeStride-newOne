/*9.สร้างฟังก์ชันสำหรับคำนวณตู้ขายน้ำ โดยต้องทอนให้ได้เหรียญหรือแบงค์น้อยที่สุด ประเภทเงินทอน
[1, 2, 5, 10, 20, 50, 100, 500, 1000] input: เงินที่ต้องจ่าย, รับเงินที่ลูกค้าจ่าย
output: เหรียญหรือแบงค์ที่ต้องทอน
ตัวอย่าง
ตัวอย่าง Input:
จำนวนเงินที่ต้องจ่าย 35 บาท ลูกค้าจ่ายเข้ามา 100 บาท
Output: ทอนด้วยแบงค์ 50 1 ใบ, เหรียญ 10 1 เหรียญ, เหรียญ 5 1 เหรียญ
*/

function changeCalcu(price, paid) {
  let change = paid - price;
  let moneyType = [1000, 500, 100, 50, 20, 10, 5, 2, 1]
  let moneyByType = {};

  for(let x of moneyType){
    let unit = Math.floor(change/x);
    if(unit>0){
        
    }
  }
}
/*
function calculateChange(ราคาสินค้า, เงินที่จ่าย) {
  let เงินทอน = เงินที่จ่าย - ราคาสินค้า;
  const ธนบัตรเหรียญ = [1000, 500, 100, 50, 20, 10, 5, 2, 1];
  const จำนวนเงินทอนแต่ละชนิด = {};

  for (let ธนบัตร of ธนบัตรเหรียญ) {
    const จำนวนเหรียญ = Math.floor(เงินทอน / ธนบัตร);
    if (จำนวนเหรียญ > 0) {
      จำนวนเงินทอนแต่ละชนิด[ธนบัตร] = จำนวนเหรียญ;
      เงินทอน -= ธนบัตร * จำนวนเหรียญ;
    }
  }

  return จำนวนเงินทอนแต่ละชนิด;
}
*/