const runner = {
  first: "Alice",
  last: "Willis",
  country: "America",
  title: "Heart Of Gold",
};
const { first, last, country } = runner;

console.log(first);
console.log(last);
console.log(country);

//console.log(title); ไม่ขึ้นเพราะไม่ได้ประกาศตัวแปร
