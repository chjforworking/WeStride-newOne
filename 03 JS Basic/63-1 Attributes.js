// เลือกปุ่มที่มี id เป็น "myButton"
let myButton = document.getElementById("myButton");

// เปลี่ยนค่าของแอตทริบิวต์ disabled ให้เป็น true (ทำให้ปุ่มคลิกไม่ได้)
myButton.setAttribute("disabled", "true");